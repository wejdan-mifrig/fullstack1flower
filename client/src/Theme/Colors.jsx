import { createTheme } from "@mui/material/styles";

const theme = createTheme({
  palette: {
    primary: {
      main: "#3e4a3a",
    },
    secondary: {
      main: "#f3efe6",
    },
    background: {
      default: "#e5e5e1",
      paper: "#ffffff",
    },
  },
});

export default theme;